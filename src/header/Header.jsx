import styles from './Header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>My Projects</h1>
      <p>React Applications & Experiments</p>
    </header>
  );
};

export default Header;
